# 一時的な試し方
about:debuggingをアドレス欄に入力する
「このFirefox」をクリック
「一時的なアドオンを読み込む」をクリック
manifest.jsonを選択する

# 拡張機能として使用
フォルダ自体ではなく中身（manifest.jsonを含む全てのファイル）を直接ZIPへ
https://addons.mozilla.org/ja/developers/　へアクセス
※80jpを使用

# backgroundとcontentのやりとり
テキストデータのみ　sendmessageなどを使用

# 